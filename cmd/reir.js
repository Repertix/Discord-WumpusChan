const Discord = require('discord.js')
module.exports.run = async (client, message, args, numero, prefix) => {
  prefix(message.author.username, "none", "reir", "frases")
  
  
}

module.exports.help = {
  name:"reir"
}
